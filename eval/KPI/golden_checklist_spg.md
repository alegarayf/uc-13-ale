# Golden Checklist — SPG KPI Agent (KPI)

| Field | Value |
|-------|-------|
| **catalog** | `uc13_ale` |
| **company** | `SPG` |
| **pipeline run_id** | `445878b36e06407385b9498dcab265c7` (`ops.retrieval_harness_runs` `run_type='pipeline'`, `harness_status='complete'`, `created_at` 2026-08-19 19:21:12.857107; correlated to KPI analysis row `2026-08-19 19:17:21`) |
| **source table** | `uc13_ale.analysis.kpi` |
| **rubric source** | `.dev/g1_score_all_agents.py::score_kpi()` (3-field; pass / partial; no `gap-correct` branch) |
| **authoring provenance** | queried `uc13_ale.analysis.kpi` WHERE `company_name='SPG'` ORDER BY `created_at` DESC LIMIT 1 -> `created_at` 2026-08-19 19:17:21.269203 |

**Verdict key:** `pass` — field meets rubric threshold; `partial` — overlay is not `healthcare_services`, healthcare-block population is below 5, or missing-KPI list is shorter than 5 / inaccurate versus corpus. SPG G1 floor is informational (`BASELINES["spg"]` unset).

## Checklist (3 rows)

| item_id | display_name | verdict | notes |
|---------|--------------|---------|-------|
| overlay_confirmed | Overlay confirmation extraction fidelity | pass | Extracted `overlay_confirmed`=`healthcare_services`, which is the `score_kpi()` pass value. Corpus corroborates a dental DSO: `3.1.1.18_Little Rock D-I.pdf` Statement 1 (Form 1120 p.1 line 26) lists `DENTAL SUPPLIES 108,214` and `DSO MGMT FEE 66,930`; `4.2.1.4_Doctor Equity Model Playbook.pdf` § "2. Attractive Financial Incentives" describes "high-dollar implant dentistry"; 22 state Business Associate Agreements (`1.1.1.1`–`1.1.1.22_*BAA.pdf`) and `5.2.2_SPG Business Associate Agreement.pdf` place the entity under HIPAA. Live `classification.company_profile.industry_overlay`=`other` (2026-07-17 row) disagrees with the KPI agent's own confirmation; the frozen rubric scores only the KPI field. |
| overlay_block_fields | Selected overlay KPI block field presence | partial | `healthcare_kpis_json` has 4 of the 7 scored fields nonempty (`census_or_patient_panel`, `utilization_or_productivity_note`, `credentialing_status_note`, `site_level_visibility`); bar is >=5, so `partial`. Populated fields are grounded — `4.1.1_Project Beam - Data Cube (as of April 2025).xlsx` sheet `CIM KPIs` / `CIM KPIs (2)` sections `Data Rows 1–22` / `1–50` carry the `Appointment Data` block (`Total Online NP Appointments Completed` / `Scheduled`) and `Total # of Starts`; `4.7.2.8_Clinical Revenue Report - Doctor Comp_Jul-24.xlsx` sheet `SUMMARY-Bonus` carries per-provider `Terminated` status. Two of the three nulls are genuine corpus absences: `LIKE '%headcount%' / '% fte%' / '%# of doctors%'` over all 394 SPG documents in `uc13_ale.ingestion.chunks` returns 0 rows. `compliance_incidents` is `[]` but `5.2.1_Regulatory - Compliance Information.pdf` (Document body) carries an "Any internal compliance audit, regulatory review or other" item with SPG responses and annual HIPAA training detail, and `8.3.1_SPG - Loss Runs_Aug-22 - Apr-25.pdf` is an Aspen American Insurance loss-run report — neither appears in the agent's `reasoning_trace` sources, so the empty list reflects retrieval reach, not established absence. (`5.2.4_SPG PHI Breach Incident Report.pdf` is a blank intake form, not a reported incident.) |
| missing_kpis_json | Missing KPI list accuracy | partial | 11 `missing_kpis_json` rows, so the `score_kpi()` length bar (>=5) is met. Escalated after sampling: the `AR Aging by Payor` row asserts "no aging schedule was present in the retrieved documents," but `2.6.2.1_Consolidated D-I AR Aging - Recon_2024.xlsx` sheet `Summary by DI Office` (section `Data Rows 1–12`) is exactly that — `Sum of 0-30 Days`, `Sum of 31-60 Days`, `Sum of 61-90 Days`, `Sum of > 90 Days` by office (e.g. Birmingham 2,090 / 0 / 0 / 3,049) plus `Sum of '-Ins Est` and `Sum of '=Patient` columns that split the balance insurance-estimate vs patient, with per-patient detail on sheet `AR Details as of 12312024`. The payor-dimension request survives, but the stated rationale is contradicted by the corpus. Samples that held: `Payor Mix` (0 chunks match `payor mix` / `payer mix` across the 394 SPG documents) and `Staff Turnover Rate` (`4.2.1.4_Doctor Equity Model Playbook.pdf` § "1. De-Risking the Investment" discusses turnover risk qualitatively; no rate stated). Length bar met; one named "missing" KPI is materially present in corpus, so not `pass`. |

**Summary:** 1 `pass`, 2 `partial` — `score_kpi()` mechanical candidate was 2/3 (`overlay_confirmed` + `missing_kpis_json` length-pass); corpus review of `missing_kpis_json` lands **1/3**.
