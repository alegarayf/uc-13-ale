# M3 backlog — fta_numeric spot-check follow-ups (2026-08-13)

Non-blocking product/eval follow-ups surfaced by the T6 human spot-check. None of these block D3 approval or the S2 warehouse write; filed for a future parallel remediation bundle (same lane as the M2 audit's WP-1–4, per `calibration-disagreement-audit-2026-08-12.md` §6.1).

| # | Item | Evidence | Affected claims | Proposed fix lane |
|---|---|---|---|---|
| 1 | **De-duplicate `revenue_by_segment_json` array construction.** Confirmed via independent follow-up validation (read-only, live Databricks, 2026-08-13): the array contains two exact byte-identical duplicated blocks — Westchester `[5]-[9]` duplicated verbatim at `[20]-[24]`, and Long Island `[10]-[14]` duplicated verbatim at `[31]-[35]` — with no `source_location` to disambiguate either block. All 20 indexed claims checked match their stated array index exactly (20/20 pass), confirming the numbers themselves are correct; only the array position is ambiguous. `addback_schedule_json` has one genuine duplicate pair (`[L]`/`[M]`, both literal `"0"`, no distinguishing citation) — `[A]`'s `"(0)"` is *not* a duplicate of these once zero-sign notation is preserved during matching (see correction note in failure-modes §1). This is the root cause of all 33 `unsupported`/LOW verdicts in this run. | `t6_draft_results.json` — `unresolvable_duplicate_array_rows` bucket, §1 failure-modes table | fta.claim.083–092, 120, 220–229, 235–239, 246–250, 267–268 (33 total) | **product** — FTA extraction agent: de-dup on write, or add a `record_id`/distinguishing locator so downstream disambiguation doesn't rely on value equality alone. |
| 2 | **Add `source_location` provenance to `revenue_by_segment_json`.** Confirmed structurally, not just empirically: the raw JSON schema for this array (`period`, `segment`, `revenue_dollars`, `source_doc`) has no `source_location` key at all — unlike `revenue_trend_json`/`gross_margin_json`/`ebitda_json`, which all carry one. So the 42 otherwise-unique claims in this array are capped at MEDIUM purely because Layer B was never structurally possible, not because of a retrieval miss. | failure-modes §1 `no_citable_chunk` bucket | fta.claim.078–108, 215–255 (subset) | **product** — extraction schema: populate `source_location` for the segment-revenue breakdown (likely from an Excel projection workbook rather than the CIM; cite the workbook/tab instead if that's the true source). |
| 3 | **Re-point the "Pro Forma Income Statement & Projection" chunk citation from the broken `027ec667…` placeholder to `cd9773ea…`.** Reconfirms M2 audit D2 (approved, WP-1 scheduled) from a second, independent verification path (live T6 spot-check rather than calibration re-run). Any tooling that resolves `source_location → chunk_id` for this section (not just the calibration sample) needs the same fix. | failure-modes §1 `known_bad_chunk_family` bucket; M2 audit `calibration-disagreement-audit-2026-08-12.md` §4 item 1 | 42 claims across `revenue_stated`/`yoy_growth_pct`/`gm_dollars_stated`/`gm_pct_stated`/`computed_from_stated` at this location | **product** — same WP-1 lane; broaden its scope statement beyond the calibration sample. |
| 4 | **New finding: `b1feca18…` (Diligence Adjusted Income Statement, CIM p.46) is internally inconsistent for 2023A/TTM columns** — the "Pro Forma Adjusted EBITDA" row is stale (missing pro forma add-back lines) while the adjacent "Margin" row is correct, so the two don't reconcile against each other for those two periods. Not previously documented in the M2 audit. | failure-modes §2 | fta.claim.062–067, 199–204 (12 claims, all still `supported`/MEDIUM via Layer A) | **product** — targeted re-extraction of CIM page 46 for the 2023A/TTM columns of this specific vision-extraction pass. |
| 5 | **T2 probe resolution-summary counts should be superseded by this session's live, location-aware verification** (`unique: 20/one_to_many: 194/unresolvable: 62` estimate → confirmed `unique-or-resolved: 241 / unresolvable: 35`) for any future planning that cites the probe's numbers as a difficulty estimate for this surface. | failure-modes §3 | n/a (methodology note) | **eval** — update `t2_fta_probe_report.json` consumers (if any) to reference this session's counts, or annotate the probe report with a forward pointer. |

**Non-goals reaffirmed (per T6 packet §1):** no `calibration.py` edits, no re-deciding rung assignments, no FTA mechanical comparator (`verify_fta_numeric` remains descoped per CHK-24). All 5 items above are read-only findings for a future parallel product/eval track, not changes made in this session.

## Operator overrides recorded against this backlog (2026-08-13)

The operator reviewed items #1–#3 above (plus several rationale-profile patterns not separately itemized in this backlog) and applied seven overrides in total to this run's scoring, rather than waiting for the product fixes. Full detail and exact claim_id lists are in `fta_numeric_elder_care_2026-08-13.review_queue.md` §"Operator overrides applied" and `draft_audit.json`'s `operator_overrides` block; summary:

- **Item #1** (duplicate array rows) → the 33 affected claims were overridden `unsupported`→`supported`. Of these, 3 (`fta.claim.120/267/268`) had a real, inspectable chunk trace and were further promoted LOW→MEDIUM/`needs_human`→`false` (override #4); the remaining 30 stayed LOW but had `needs_human` cleared to `false` once the exact duplicate-block root cause was independently confirmed (override #7 — Westchester `[5]-[9]`≡`[20]-[24]`, Long Island `[10]-[14]`≡`[31]-[35]`, 20/20 indexed values verified). Item #1's de-duplication fix is still open; once landed, a future spot-check run should resolve these to a clean unique path without needing an override.
- **Item #2** (missing `source_location` on `revenue_by_segment_json`) → the 42 affected claims stay MEDIUM (no chunk-verification layer exists) but had `needs_human` cleared to `false` (override #6), since Layer A is self-consistent/unique-path for all 42 and the missing citation is a confirmed schema-level gap, not a retrieval failure. Item #2's schema fix is still open.
- **Item #3** (broken `027ec667…` chunk citation) → 42 claims overridden MEDIUM→HIGH (`needs_human`→`false`, override #2) on the basis that sibling chunk `cd9773ea…` verbatim-confirms the same values; a further 18 claims with their own correct, non-substituted `871ba744…` citation were also promoted MEDIUM→HIGH (override #3) since the one_to_many flag was solely due to the same value recurring at the duplicate-extraction location. Item #3's citation-remapping fix is still open; once landed, these claims would resolve to HIGH mechanically rather than via override.
- **Additional generalized review** (override #5, 31 claims: boolean provenance flags, chunk-present-but-not-verbatim, composite multi-line-item) → `needs_human`→`false`, confidence unchanged, after the operator reviewed a representative sample of each rationale profile and confirmed the treatment generalizes to every claim sharing that profile.

None of these overrides change `financial_trends`, the chunk store, or any calibration/registry artifact, and none pre-empt the product fixes above — they are scoped to *this run's scoring only*. After all seven overrides, `needs_human=0/276`; confidence bands (HIGH=170/MEDIUM=76/LOW=30) were left untouched by overrides #5–#7, since those only resolved the human-review flag, not evidentiary strength.
