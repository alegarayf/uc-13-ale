# M3 backlog — T5 exec_summary spot-check follow-ups (2026-08-12 session)

Derived from `exec_summary_elder_care_2026-08-12.failure_modes.md`. Per charter: M3 = harness + human rubric execution path; consumes M2's recorded `human`/`deterministic` rung assignments read-only. Re-calibration and any item-26-triggered judge re-measurement are **post-M3**, gated on the M2 audit's §6.3 loop-back trigger (WP-1–3 landed + M3 harness exists + operator authorizes). Nothing below reopens M2 or asks M3 to re-decide rungs.

## Product track (parallel, does not block M3 — same lane as M2 audit's §6.1 bundle)

1. **[New, high-value] Add an `analysis.*` structured-table lookup to the spot-check evidence-fetch path**, alongside (not instead of) VDR chunk RAG, for `exec_summary` (and any other surface whose claims summarize a workstream's own computed output: ratings, confidence, ledgers, ranked issues, reconciliation summaries). This is the fix for `retrieval_scope_gap`, which accounted for 27 of 28 overturned claims this session. Concretely: for `exec_summary`, fetch `diligence_report.{section_ratings_json, section_confidence_json, top_10_issues_json, reconciliation_summary_json}` and the per-workstream tables (`business_model`, `kpi`, `financial_trends`, `quality_of_earnings`, `forecast`) as a second candidate-evidence source. **Priority: high** — this is the single highest-leverage fix found this session; it should materially improve any future judge/calibration run on this surface without touching the exec_summary generator itself.
2. **[New] Fix `source_ref` mislabel in the `exec_summary` presentation packet.** `prepare_spot_check` currently emits `source://uc13_ale.analysis.legal.executive_summary`; the real source is `uc13_ale.analysis.diligence_report.executive_summary`. Low effort, removes a standing citation bug for every future exec_summary spot-check. **Priority: medium.**
3. **[Already scheduled, carry forward — M2 audit WP-2]** Fix the ~1,200-character chunk-text truncation in the evidence-fetch driver (`eval/content/calibration.py: fetch_chunk_metadata`/`retrieve_evidence`). This session reconfirmed the same truncation symptom on exec_summary claims 008–010, 018 (operator flagged it independently as "chunk boundary" in review notes). No new scope; just re-affirming it's real and reproducible outside the FTA surface too. **Priority: medium** (already owned by M2 audit's parallel remediation bundle).
4. **[New, low priority] Investigate claim.026's generation.** The executive_summary's own "Confidence" paragraph correctly states "five ... carry a Red rating ... remaining two rated Yellow" (7 of 7 colored), but the extracted claim.026 restates this as "five of seven ... carry at least one Red or Yellow rating" — an undercount. Check whether the claim-extraction step for this one rollup sentence has a systematic pattern (e.g. conflating "Red-only count" with "Red-or-Yellow count") before assuming it's a one-off. **Priority: low** (single confirmed instance this session).

## Eval / harness track (M3 scope — item 27 + human rubric path)

5. Fold the reconciled verdicts (`exec_summary_elder_care_2026-08-12.verdicts.yaml`, `status: operator_approved`) into the M3 harness's evidence set once the operator authorizes `write_spot_check_results` for this run. This is the harness-consumable output the charter's S2 build expects from a T5-shaped human rubric pass.
6. When the M3 harness (item 27) is built, its evidence-fetch step for `exec_summary`-shaped surfaces should route through fix #1 above (or an equivalent dual-source lookup) — don't let the harness inherit the chunk-RAG-only blind spot this session just diagnosed. This is a harness-design input, not a re-opening of M2 rungs.
7. No sample re-authoring and no rung changes this session — consistent with the M2 audit's D3 disposition (structural/rollup claims stay rung-3 human review; this session shows *how* to do that human review correctly, it doesn't argue for a rung upgrade).

## Explicitly NOT in this backlog (post-M3 / out of scope)

- Re-running `eval.content.calibration` for `exec_summary` — gated on M2 audit §6.3 (WP-1–3 landed + M3 harness exists + operator authorization). Fix #1 above is a strong candidate input to that future re-calibration, but re-calibration itself stays post-M3.
- Any exec_summary generator/prompt changes — this session found the generator's claims are well-grounded in the analysis tables; no generator defect was identified (claim.026 is a claim-*extraction* artifact for the spot-check sample, not a generator defect in the exec_summary text itself, which states the correct 5-Red/2-Yellow breakdown).
- Item-26 human-spot-check re-trigger / rung upgrade arguments — out of scope per charter; not raised by anything in this session.
