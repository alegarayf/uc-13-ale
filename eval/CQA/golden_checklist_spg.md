# Golden Checklist — SPG Customer Quality Agent (CQA)

| Field | Value |
|-------|-------|
| **catalog** | `uc13_ale` |
| **company** | `SPG` |
| **pipeline run_id** | `445878b36e06407385b9498dcab265c7` (W2c promotion target; `ops.retrieval_harness_runs` `run_type='pipeline'`, `harness_status='complete'`, created `2026-08-19 19:21:12.857107`) |
| **source table** | `uc13_ale.analysis.customer_quality` |
| **rubric source** | `.dev/g1_score_all_agents.py::score_cqa()` (6-field; pass / partial / gap-correct) |
| **authoring provenance** | queried `uc13_ale.analysis.customer_quality` WHERE `company_name='SPG'` ORDER BY `created_at` DESC LIMIT 1 -> `created_at` 2026-08-19 19:15:06.439029. Verdict justifications written from `uc13_ale.ingestion.chunks` (`company_name='SPG'`, 44,038 chunks / 394 files) — source documents cited by file, page or sheet, and `chunk_id`; the analysis row was read only to know which fields exist. |

**Verdict key:** `pass` — field meets rubric threshold; `partial` — field partially populated or thinly grounded; `gap-correct` — absence correctly surfaced (empty concentration list, all-null retention with discrepancies, or valueless payor dict). SPG G1 floor is informational (`BASELINES["spg"]` unset). SPG is a non-banked deal with no CIM; "customers" in this corpus are SPG's own practice locations, since no patient-level revenue schedule exists.

## Checklist (6 rows)

| item_id | display_name | verdict | notes |
|---------|--------------|---------|-------|
| concentration | Customer concentration extraction | pass | `top_customers_json` is a nonempty 10-row list, so `score_cqa()` is `pass`. The underlying balances are in `2.6.2.1_Consolidated D-I AR Aging - Recon_2024.xlsx` sheet Summary by DI Office (chunk `3457d61f`): Indiana `> 90 Days 98,165.74`, `Total 104,010.74`, `'-Ins Est 75,740.20`, net-to-NS `28,270.54`; Valley `61-90 11,930.01`, `> 90 16,756.40`, `Total 30,028.40`; Louisville `Total 13,143.50`, `> 90 3,694`; Springdale `Total 7,352`; Birmingham `Total 5,139`, `> 90 3,049`; Columbus `3,206` entirely in `> 90`; Grand Junction `2,230` entirely in `> 90`; Cincinnati `Total 2,537`, `> 90 377`; Philadelphia `Total 1,338` with `0` in `> 90`; Tacoma `350` entirely in `> 90`. Sheet AR RECON OD TO NETSUITE (chunk `5d642859`) supplies Spokane D&I `NS 12100 AR 8,000` with no aging split, matching the extraction's "no aging bucket breakdown available". Caveat: scans for `customer concentration`, `revenue concentration`, and `top 10 customers` returned 0 rows across the SPG corpus, so no revenue-by-customer schedule exists and this ranking is an AR-balance proxy over SPG's own practice entities, not a patient-revenue ranking — the agent's own first discrepancy says the same, and T7 should re-sample this row. |
| retention | Retention metrics extraction | gap-correct | `retention_json.nrr_pct`, `grr_pct`, and the rubric's `logo_churn_pct` key are all null (the row carries `logo_churn_rate_annual_pct`, also null) and `discrepancies_json` is present, so `score_cqa()` takes the all-null `gap-correct` branch. The absence is real, not a retrieval miss: an independent scan of all 44,038 SPG chunks for `net revenue retention`, ` nrr `, `gross revenue retention`, and `logo churn` returned 0 rows. Episodic implant and denture procedures have no renewal series to retain, consistent with `4.2.2.1_Shared Practices Dashboard_2024.pdf` p.2 (chunk `af5c2ee7`), which measures location performance by new patient visits over a fixed Model Date Range rather than by cohort retention. |
| customer_tenure | Customer tenure extraction | partial | `customer_tenure_json.tenure_distribution_note` is null, and the rubric has no `gap-correct` branch for this field, so `score_cqa()` is `partial`. The null is substantively correct: scans for `average tenure`, `avg. tenure`, `patient tenure`, and `years avg` returned 0 rows across the SPG corpus. The nearest sourced measure is new-patient visit volume over the Buxton Model Date Range 6/1/2023–5/31/2024 (`4.2.2.1_Shared Practices Dashboard_2024.pdf` p.2, chunk `af5c2ee7`), which is throughput rather than tenure. The lost point reflects the rubric's shape, not a missed document — recorded here so a later re-run is not read as an improvement opportunity in the corpus. |
| payor_mix | Payor mix extraction | partial | `payor_mix_json` is a 6-row list (Medicare, Medicaid, VA, Commercial, Managed Care, Other) with every `pct_of_revenue` null and every `source_doc` null; a list without values is `partial` (not `gap-correct`) under `score_cqa()`. Third-party payor involvement is genuinely documented — `2.1.1.1_Shared Practices Financial Statements_2022.pdf` p.11–12 Patient Receivables (chunk `deac7235`) describes "uncollateralized patient and third-party payor obligations" and identifying trends "for each of its major payor", and p.12–13 Patient Service Revenue (chunk `b5927c97`) reports revenue net of amounts from "patients, third-party payors, and others" — but scans for `payor mix` and `payer mix` returned 0 rows and no Medicare/Medicaid/commercial percentage split exists anywhere in the corpus. The six categories are therefore unsourced scaffolding; `partial` is the correct landing. |
| discrepancies_json | Discrepancies correctly reported | pass | `discrepancies_json` is a 5-entry list, meeting the rubric's `len >= 3`, so `pass`. Two entries were independently verified against source. Birmingham: `2.6.2.1_Consolidated D-I AR Aging - Recon_2024.xlsx` sheet AR RECON OD TO NETSUITE (chunk `5d642859`) shows Birmingham D&I `NS 12100 - Accounts Receivable (A/R) = -26,767.29` against `OD Reported office TOTAL AR 5,139` and `OD Total 90+Days 3,049` — a credit NetSuite balance the reconciliation does not explain. Indiana: the same sheet shows `NS 12100 = 28,320.54` versus `OD Reported office TOTAL AR 28,270.54` with the note "Legacy Insurance AR has always been excluded from NS balances.", and sheet Summary by DI Office (chunk `3457d61f`) shows the `'-Ins Est 75,740.20` wedge inside the `104,010.74` OD total, carrying its own note "Legacy AR Ins Balance has always been Excluded from NS". |
| data_room_gaps | Data-room gaps correctly reported | pass | `data_room_gaps` is a list, which is all `score_cqa()` tests, so `pass`. Caveat, and the main finding for T7 on this checklist: both gap strings ("Termination-for-convenience terms not determinable", "Change-of-control consent terms not determinable — contract review required") understate the corpus. Over 40 Management/Master Services Agreements are ingested under `1.1.3.1.*`, and their term and termination articles are explicit: `1.1.3.1.11_Indiana MSA.pdf` p.5 §5 Term and Termination (chunk `e328f4de`) states an initial term "for a period of fifteen (15) years commencing on the Effective Date" with automatic one-year renewal terms, and `1.1.3.1.29_Durham MSA.pdf` p.11–13 TERM AND TERMINATION §8.1 (chunk `3804c4a5`) states the same fifteen-year initial term with annual auto-renewal; assignment and prospective-event provisions sit in the same agreements (`1.1.3.1.10_Grand Junction MSA.pdf` p.10–13 §8.9, chunk `58edf365`). What is genuinely absent is patient-level contract terms — these MSAs run between each Sub-DSO contractor and its practice entity, not between SPG and its patients — so the gap is real only once restated at the patient layer. The rubric tests list type only, so the verdict stands at `pass`. |

**Summary:** 3 `pass`, 2 `partial`, 1 `gap-correct` — `score_cqa()` **3/6**.
