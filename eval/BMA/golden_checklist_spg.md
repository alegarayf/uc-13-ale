# Golden Checklist — SPG Business Model Agent (BMA)

| Field | Value |
|-------|-------|
| **catalog** | `uc13_ale` |
| **company** | `SPG` |
| **pipeline run_id** | `445878b36e06407385b9498dcab265c7` (W2c promotion target; `ops.retrieval_harness_runs` `run_type='pipeline'`, `harness_status='complete'`, created `2026-08-19 19:21:12.857107`) |
| **source table** | `uc13_ale.analysis.business_model` |
| **rubric source** | `.dev/g1_score_all_agents.py::score_bma()` (7-field; pass / partial / gap-correct; only `sales_motion` can be `gap-correct`) |
| **authoring provenance** | queried `uc13_ale.analysis.business_model` WHERE `company_name='SPG'` ORDER BY `created_at` DESC LIMIT 1 -> `created_at` 2026-08-19 19:20:52.6751. Verdict justifications written from `uc13_ale.ingestion.chunks` (`company_name='SPG'`, 44,038 chunks / 394 files) — source documents cited by file, page or sheet, and `chunk_id`; the analysis row was read only to know which fields exist. |

**Verdict key:** `pass` — field meets rubric threshold; `partial` — field empty or thinly populated; `gap-correct` — sales-motion absence correctly surfaced (tag and JSON both empty). SPG G1 floor is informational (`BASELINES["spg"]` unset). SPG is a non-banked deal: the corpus is a full VDR with no CIM, so citations are to financial statements, playbooks, the projection model, and analytics reports rather than to CIM pages.

## Checklist (7 rows)

| item_id | display_name | verdict | notes |
|---------|--------------|---------|-------|
| products_services | Products and services extraction | pass | `products_services_json` is a nonempty 4-row list, so `score_bma()` is `pass`. Fee-for-service clinical revenue is documented at the practice level: `3.1.1.11_Valley D-I.pdf` p.44 Profit and Loss (chunk `9e606159`) reports `40100 Fee for Service Income 2,070,148.25`, `40400 Adjustments/Write-offs -41,465.86`, `Total 40000 Revenue $2,028,682.39`, `41000 Collections Adjustment 674,621.21`, `Total Income $2,703,303.60`. Platform-level economics: `2.3.1_Project Beam - Projection Model.xlsx` sheet Consolidated Summary (chunk `27f64d31`) carries `Total Pro Forma Maturity Adj. EBITDA - 4-Wall` 4,311.83 (2022A) / 11,416.12 (2023A) / 19,327.34 (2024A) / 22,059.18 (2025E) in USD thousands, and (chunk `0744164f`) `(-) Total Corporate Expenses` 2,770.36 / 5,753.88 / 6,595.76 / 6,633.85. Unit economics: `4.2.1.12_SPG Finance Playbook- _rn - New Location Ramp.pdf` p.1 De Novo Unit Economics (chunk `67a2e41d`) states Avg. Net Investment ~$225K, Avg. Gross Buildout ~$265K, Payback ~8–10 months, Year 1 Revenue ~$1.45M, Year 1 4-Wall EBITDA ~$350K. Caveat: the extracted `$12,731.6K` 2022 figure is the consolidated Pro Forma Maturity Adj. EBITDA line, not the 4-Wall line quoted above; the sheet carries both, so the label matters when this row is re-sampled. |
| people_org | People and organization extraction | pass | `people_and_org_json` is a nonempty dict, so `score_bma()` is `pass`. The corpus documents the C-suite and the equity structure directly: `7.1.1_Org Chart.pdf` p.1 (chunks `5c88ed84`, `b1b05bb0`) is headed `Dr. Alex Sharp Chief Executive Officer` with `Dr. George Hariri Chief Financial Officer / Chief Marketing Officer` and `Dr. Matt Guarino Chief Operating Officer` over VP Finance / VP Accounting / VP Marketing and VP Operations / RM Team / SPG Smile Consultants. `1.3_SPG DSO Structure Chart.pdf` p.1–3 (chunks `1833e653`, `6e39449b`, `f108f843`) maps The Billion Dollar Podcast, LLC and Shared Practices Holdings over a Master DSO with per-state Sub-DSOs (SP Practice Group MN/NE/WA/IN/SC/NC/VA, LLC) and EINs, and states `~98.6%`. `1.2_BDP Cap Table_Mar-25.xlsx` sheet SPG (chunk `754648c3`) shows The Billion Dollar Podcast, LLC holding 99,618.2339948 Class A units = `0.986221502770218` of Shared Practices Group, LLC; sheet BDP (chunk `cfd6a3be`) shows Guarino Ventures, LLC and Hariri Business Services, LLC each at `0.1667`; sheet Practices (chunk `bddf9fd9`) lists 39 active practice entities with member and Succession Agreement Status. Caveat: the extracted field instead names De Novo Playbook task owners (`4.2.1.3_De Novo Playbook.pdf` p.20, chunk `2cdb6617`) and partnership loss allocations, and does not reach this Org Chart or cap table; the rubric tests only for nonempty, so the verdict stands at `pass`. |
| customer_profile | Customer profile extraction | pass | `customer_profile_json` is a nonempty dict, so `score_bma()` is `pass`. Footprint is sourced: `4.2.2.1_Shared Practices Dashboard_2024.pdf` p.2 Location Data (chunk `af5c2ee7`) states "Buxton received data for 38 Shared Practices locations, 34 of which are currently open" and defines the Model Date Range as new patient visits 6/1/2023–5/31/2024. Segmentation is sourced: `4.2.2.2_Shared Practices Optimizer Results_2024.pdf` p.29 PATIENT SEGMENTS: GROUP C (chunks `84d4ebed`, `3cf86fa0`) names Segment C11 Sophisticated City Dwellers, C12 Golf Carts and Gourmets, C13 Philanthropic Sophisticates, C14 Boomers and Boomerangs. Acquisition economics: `4.2.1.12_SPG Finance Playbook- _rn - New Location Ramp.pdf` p.2–3 Patient Acquisition Strategy (chunk `85c14b7c`) states initial focus paid search, system-wide CAC goal <$600, ROAS benchmark ~7.8x. |
| sales_motion | Sales motion extraction | pass | `sales_motion_tag`=`relationship` and `sales_motion_json` is nonempty, so `score_bma()` is `pass` (not the `gap-correct` branch). `4.2.1.13_Consultative Sales Process Playbook.pdf` p.1 §1 Initial Contact (chunk `166b879a`) states "Connection Center receives patient phone call" and "Schedule initial consultation", with metrics Appointment scheduling rate / No Show Rate / Show Rate / Missed Call Rate — a consultative, human-led motion, consistent with the `relationship` tag rather than `inbound_plg`. `4.2.1.9_Marketing Playbook - SPG Smile Journey.pdf` p.1 (chunks `b814b514`, `afad1943`) describes the branded journey from initial consultation through treatment planning and ongoing care. |
| key_dependencies | Key dependencies extraction | pass | `key_dependencies_json` is a nonempty 12-row list, so `score_bma()` is `pass`. Platform dependencies are sourced: `4.2.1.2_Data Centraliztion Playbook.pdf` p.1 §1 Systems Integration (chunk `b8ffad6f`) states "We centralized data flows by integrating all key operational systems with NetSuite, Oracle, and Open Dental"; `9.2_SPG Tech Stack.pdf` p.1 (chunks `8629fb44`, `d71b0be1`, `02a254b9`, `343e8e37`) enumerates NetSuite ERP, Paylocity, Monday.com, and OpenDental. Communications stack: `2.5.2.8_RingCentral Invoice_Jul-24.pdf` p.1 Statement Summary (chunk `16d9b18a`) lists DigitalLine Unlimited Ultra $3,125.00, Live Reports License $180.00, and Additional Local Number $114.00; the per-line quantity columns the extraction quotes (125 / 127 / 30 / 171) sit in the p.3 Statement Details table (chunk `2523bf25`) and were not re-quoted in full here. Key-person dependency: `4.2.1.11_SPG Finance Playbook - Cash Flow Culture.pdf` p.2 Cultural and Intangible Benefits (chunk `fe25bdc5`) states "POMs and RMs have fully embraced the change" and that a formulary of approved vendors and prices drove compliance improvement. |
| data_room_gaps | Data-room gaps correctly reported | pass | `data_room_gaps` is a list, which is all `score_bma()` tests, so `pass`. The first gap string ("No CIM or Offering Memorandum detected ... appears to be a non-banked deal") is independently confirmed: a filename scan of `uc13_ale.ingestion.chunks` for `%cim%` / `%offering memo%` / `%confidential inf%` returned 0 rows, and a body scan for `confidential information memorandum` returned 0 rows, across all 44,038 SPG chunks in 394 files. Caveat: the second gap string is an LLM truncation notice ("LLM response was truncated by the token limit ... Consider raising max_tokens"), which is pipeline health rather than a data-room gap; the rubric checks list type only, so it does not move the verdict, but it should not be read as a corpus finding. |
| overlay_conflict | Overlay conflict correctly reported | pass | `overlay_conflict`=`false`, so `score_bma()` is `pass`. The corpus business is dental implant and denture care billed as patient service revenue: `2.1.1.1_Shared Practices Financial Statements_2022.pdf` p.12–13 Patient Service Revenue (chunk `b5927c97`) and p.11–12 Patient Receivables (chunk `deac7235`); `3.1.1.11_Valley D-I.pdf` p.44 (chunk `9e606159`) books `40100 Fee for Service Income`. The live `uc13_ale.classification.company_profile` SPG row read at authoring time (`created_at 2026-07-17 20:23:18.932297`) carries `industry_overlay='other'`, `vertical_subsector` null, `revenue_model='hybrid'`, `banked='false'` — no healthcare overlay is asserted, so there is nothing for the extracted dental model to conflict with. That profiler row predates this milestone's SPG profiler re-run and is not treated here as the current profile. |

**Summary:** 7 `pass`, 0 `partial`, 0 `gap-correct` — `score_bma()` **7/7**.
