# Golden Checklist — SPG Company Profiler (PROFILER)

| Field | Value |
|-------|-------|
| **catalog** | `uc13_ale` |
| **company** | `SPG` |
| **pipeline run_id** | T1 job `217099929033112` (Profiler output is `classification.company_profile` and is not keyed by the 2026-08-19 diligence `445878b36e06407385b9498dcab265c7`; cited for T5 scoring against T1's fresh row) |
| **source table** | `uc13_ale.classification.company_profile` |
| **rubric source** | `.dev/g1_score_all_agents.py::score_profiler()` (7-field; pass / partial; no `gap-correct` branch) |
| **authoring provenance** | queried `SELECT CAST(created_at AS STRING) AS ts, industry_overlay, overlay_confidence, revenue_model, revenue_model_note, business_description, deal_type, banked, banked_note, vertical_subsector, data_room_gaps FROM uc13_ale.classification.company_profile WHERE company_name = 'SPG' ORDER BY created_at DESC LIMIT 1` -> `created_at` 2026-08-25 18:14:01.971483 (post-T1; not the pre-T1 stale `2026-07-17` row) |

**Verdict key:** `pass` — field meets `score_profiler()` threshold; `partial` — overlay is not `healthcare_services`, a presence field is empty, `banked` is not true, or `data_room_gaps` is not a JSON list. SPG G1 floor is informational (`BASELINES["spg"]` unset).

## Checklist (7 rows)

| item_id | display_name | verdict | notes |
|---------|--------------|---------|-------|
| industry_overlay | Industry overlay extraction | pass | Extracted `industry_overlay`=`healthcare_services`, `overlay_confidence`=`high` (equals the frozen `score_profiler()` pass token). Independent: `2.1.1.1_Shared Practices Financial Statements_2022.pdf` p.12 Patient Service Revenue (chunk `b5927c97`) reports patient-care receipts at net realizable amounts from patients and third-party payors; `2.1.2_Billion Dollar Podcast Financial Statements_2023.pdf` p.5 (chunk `190307d1`) books Patient service revenue `$ 33,471,273` against Coaching and consulting `2,659,233`; the DSO chart names Valley / Birmingham / Springdale Dentures and Implants practices. The pre-T1 stale row had `industry_overlay`=`other`; this post-T1 row is the first SPG profile that clears the healthcare token. |
| revenue_model | Revenue model extraction | pass | Extracted `revenue_model`=`repeat_services` (truthy, so `score_profiler()` is `pass`). Warehouse `revenue_model_note` is the string `null`. Independent: `4.2.1.9_Marketing Playbook - SPG Smile Journey.pdf` p.1 (chunk `b814b514`) guides patients "from their initial consultation to ongoing care"; p.2 (chunk `fd154caf`) "encourages repeat business and maximizes the lifetime value". Practice-level `3.1.1.11_Valley D-I.pdf` p.44 (chunk `9e606159`) books `40100 Fee for Service Income 2,070,148.25`. Caveat: the 2023 BDP statements also carry a coaching/consulting line, so a hybrid read is available; the rubric scores presence only. The stale row had `hybrid`. |
| business_description | Business description extraction | pass | `business_description` nonempty (pass). Independent: `1.3_SPG DSO Structure Chart.pdf` p.4 (chunk `e6e00cf6`) labels the Master Services Agreement "Service provider to Practices for administrative staffing, administrative support, office space and equipment leasing, marketing, IT and other DSO services" — the extracted prose is a near-verbatim of that caption. `1.1.3.1.10_Grand Junction MSA.pdf` p.14 Exhibit A (chunks `3d0508d8`, `c0a502a2`) leases dental office space and establishes accounting procedures, controls, and financial statements for the Practice. |
| deal_type | Deal type extraction | pass | Extracted `deal_type`=`buyout` (truthy, so `score_profiler()` is `pass`). Independent: no ingested CIM or process letter names `buyout` versus `growth_equity`. `1.1.2.3_Shared Practices, LLC - Operating Agreement (Amended).pdf` p.39 §10.7.2 (chunk `1c12438a`) is a drag-along sale of a majority of Class A/B/C Units to a Third Party Purchaser — a control transaction, not an enum label. The rubric scores presence, not enum fidelity. |
| banked | Banked status extraction | partial | Extracted `banked`=`false`; `banked_note` is the `detect_banked` no-CIM string. Independent of the LLM field: a filename scan of `uc13_ale.ingestion.chunks` (`company_name='SPG'`, 44,038 chunks / 394 files) for `%cim%` / `%offering memo%` / `%confidential inf%` returned 0 rows; a body scan for `confidential information memorandum` returned 0 rows; `uc13_ale.classification.doc_relevance` BUSINESS_MODEL filenames matching those tokens returned 0 (the three BM-tagged files are `1.3_SPG DSO Structure Chart.pdf`, `9.2_SPG Tech Stack.pdf`, `9.1_SPG Domain Names_Apr-25.xlsx`). `detect_banked`'s CIM-keyword rule therefore returns False. This `partial` is a true non-banked finding, not a write-path artifact. |
| vertical_subsector | Vertical subsector extraction | pass | Extracted `vertical_subsector`=`dental` (truthy, so `score_profiler()` is `pass`). Independent: DSO-chart practice names are Dentures and Implants entities; Grand Junction MSA p.14 leases "dental office space"; `4.2.2.1_Shared Practices Dashboard_2024.pdf` p.2 Location Data (chunk `af5c2ee7`) states Buxton received data for 38 Shared Practices locations, 34 currently open; Smile Journey p.2 names "specialized expertise in implants and dentures". |
| data_room_gaps | Data-room gaps field presence | partial | Warehouse `data_room_gaps` is SQL NULL. Profiler writes `None` when the in-memory gap list is empty (`data_room_gaps if data_room_gaps else None`). `score_profiler()` awards `pass` only when `json.loads` yields a list, so NULL is `partial`. Independent: the six other scored fields are populated, so an empty gap list is the expected write; the no-CIM fact is recorded on `banked` / `banked_note`, not here. This is the same empty-to-NULL write-path as the GKF and Clearsulting profiler checklists. |

**Summary:** 5 `pass`, 2 `partial` — `score_profiler()` **5/7**. SPG G1 floor is informational.
