# Aggregate-exclusion population (Contract T3-b / T13) — company-scoped machinery.
# Launch population: profiler (no analysis table) + zero-claim / no_citation_source KPI intents.
# Clearsulting claim mappings (RB-DEFECT-T9-1) live in kpi_claim_intent_map.yaml; Clearsulting block is empty.
companies:
  elder_care:
    excluded:
      - intent_id: profiler.company_size_indicators
        exclude_reason: no_citation_source
      - intent_id: kpi.retrieve_healthcare_labor_market
        exclude_reason: no_citation_source
      - intent_id: kpi.retrieve_bill_rates_and_margins
        exclude_reason: no_citation_source
      - intent_id: kpi.retrieve_headcount_attrition
        exclude_reason: no_citation_source
      - intent_id: kpi.retrieve_pipeline_backlog
        exclude_reason: no_citation_source
  clearsulting:
    excluded: []
