# Aggregate-exclusion population (Contract T3-b) — machinery only; gold refresh is T4.
# Launch population: profiler (no analysis table) + zero-claim KPI intents (§5.2 item 3).
# Clearsulting claim mappings (RB-DEFECT-T9-1 / Option A, 2026-08-14) removed bill_rates,
# headcount_attrition, and pipeline_backlog from exclusion — they now have live claim targets.
excluded:
  - intent_id: profiler.company_size_indicators
    exclude_reason: no_citation_source
  - intent_id: kpi.retrieve_healthcare_labor_market
    exclude_reason: no_citation_source
