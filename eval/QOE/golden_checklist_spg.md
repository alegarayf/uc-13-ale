# Golden Checklist — SPG Quality of Earnings Agent (QOE)

| Field | Value |
|-------|-------|
| **catalog** | `uc13_ale` |
| **company** | `SPG` |
| **pipeline run_id** | `445878b36e06407385b9498dcab265c7` (`ops.retrieval_harness_runs` `run_type='pipeline'`, `harness_status='complete'`, `created_at` 2026-08-19 19:21:12.857107; correlated to QOE analysis row `2026-08-19 19:22:38`) |
| **source table** | `uc13_ale.analysis.quality_of_earnings` |
| **rubric source** | `.dev/g1_score_all_agents.py::score_qoe()` (N=6 structural rows; pass / partial / gap-correct; only `qofe_report_present` can be `gap-correct`) |
| **authoring provenance** | queried `uc13_ale.analysis.quality_of_earnings` WHERE `company_name='SPG'` ORDER BY `created_at` DESC LIMIT 1 -> `created_at` 2026-08-19 19:22:38.698323 |
| **candidate_total M (Decision M2-C)** | **5** — re-derived live, not inherited. FTA precondition field `uc13_ale.analysis.financial_trends.addback_schedule_json` for SPG is the empty JSON array `[]` (single SPG row, `created_at` 2026-08-19 19:20:58.148507), so the presence bar **fails** and `M = N - 1`, excluding `tier_classification_fidelity` from the scoring denominator. Corroborating `uc13_ale.analysis.quality_of_earnings.addback_ledger_json` is also `[]` with `tier4_addback_count`=`0`. This is a genuine deviation from GKF's and Clearsulting's `M=6`, not a copy-paste error. |

**Verdict key:** `pass` — field meets rubric threshold; `partial` — below threshold, or QofE report present (`score_qoe()` never awards `pass` when `qofe_report_present` is true), or `tier4_addback_count` != ledger length; `gap-correct` — `qofe_report_present` is false. SPG G1 floor is informational (`BASELINES["spg"]` unset).

## Checklist (6 rows)

| item_id | display_name | verdict | notes |
|---------|--------------|---------|-------|
| revenue_quality_flags | Revenue quality flags extraction | pass | 4 `revenue_quality_flags_json` entries (>=3). Grounded in corpus: `2.5.3.3_Deferred Revenue FY_2024.xlsx` sheet `SPGlBalanceSheetDetail` section `54350 - Deferred Revenue` carries the FY2024 total `14,543,781.09` across the practice columns; `2.3.1_Project Beam - Projection Model.xlsx` sheet `Mature Revenue Calc` carries `213-DENVER D&I` FY2024 `1,532.5` vs FY2025 `720` ($000s) and the negative month `217-PHILADELPHIA D&I 2024-08-31 = -17.222`. All four flags are `Yellow` severity with `source_doc` populated. |
| ebitda_scenarios | EBITDA scenarios computation | partial | `score_qoe()`'s structural check (`isinstance(dict) and dict`) awards `pass` because the dict is nonempty, but every scenario value is null: `reported_ebitda`=`null`, `tier1_plus_tier2_ebitda`=`null`, `tier1_only_ebitda`=`null`, `tier1_addback_total`=`0`, `tier2_addback_total`=`0` — the only populated key is the boilerplate `note`. No scenario was computed. The corpus does state a consolidated EBITDA the agent could have anchored on: `2.3.1_Project Beam - Projection Model.xlsx` sheet `Consolidated Summary` section `Summary` gives `Total Pro Forma Maturity Adj. EBITDA` 2024 Actual `12,731.59` ($000s) and `Total Pro Forma Maturity Adj. EBITDA - 4-Wall` 2024 Actual `19,327.34`, repeated on sheet `Consolidated - Base Business`. Corpus review downgrades the structural pass. |
| pre_qofe_scope | Pre-QofE scope items extraction | pass | 9 `pre_qofe_scope_items_json` entries (>=5), each naming a document and figure that verify independently: `3.1.1.18_Little Rock D-I.pdf` Statement 1 / Statement 2 `DSO MGMT FEE 66,930`; `3.1.2.2_Shared Practices Group, LLC.pdf` Schedule M-1 line 1 `Net income (loss) per books -7,126,075` and Part I line 4a `-7,126,075` with Schedule L balance sheets; `3.1.1.16_Sharp Family Dentistry.pdf` § `Expenses` `61130 Owner Family Payroll Taxes 2,679.82` and `61820 Owner Payroll Taxes 10,271.06`; the deferred-revenue roll-forward request ties to the `54350` balance above. `related_addback_ids` is empty on every item, consistent with an empty ledger. |
| qofe_report_present | QofE report presence detection | gap-correct | `qofe_report_present`=`false`, which `score_qoe()` maps to `gap-correct`. No QofE report document exists among the 394 SPG documents in `uc13_ale.ingestion.chunks`, and the agent's `retrieve_qofe_report` step returned 0 chunks — so the gap call is correct as to the report itself. The corpus does show a third-party QofE was performed and its adjustments were consumed: `4.1.1_Project Beam - Data Cube (as of April 2025).xlsx` sheet `Service Mix` (section `Data Rows 901–905`) states "Source: Firethorn Quality of Earnings," and `2.3.1_Project Beam - Projection Model.xlsx` sheet `ADJ` carries ~40 per-entity line items named `11111 - Change in Deferred Revenue per QofE` (e.g. `206-Valley D&I - Chandler`, `213-Denver D&I`, `249-Kansas D&I`). The undelivered Firethorn QofE is a sharper data-room ask than the recorded gap note. |
| tier_classification_fidelity | Addback tier classification fidelity | partial | `addback_ledger_json` is `[]` and `tier4_addback_count`=`0`; the `isinstance(list) and ledger` branch is false, so `score_qoe()` returns `partial`. **This is the item excluded by the Decision M2-C precondition adjustment** (FTA `addback_schedule_json` = `[]`), so it sits outside the `M=5` denominator. Corpus note: addback source material does exist — `2.5.2.4_Legal Expense Addbacks_Jan-23 - Sep-24.xlsx` (sheets `Sheet1` / `Sheet2`, with a `Legal Dept Brief Explanation` / `Memo` column per item) and the QofE-derived `ADJ` tab above — so the empty ledger traces to an empty upstream FTA passthrough, not to an addback-free deal. |
| data_room_gaps | Data-room gaps correctly reported | pass | 3 `data_room_gaps` entries, list-typed, which meets `score_qoe()`: no per-customer gross margin for the CQA cross-check, no reported EBITDA base for the addback percentage, and no QofE report in the VDR. The second and third entries are consistent with the `ebitda_scenarios` nulls and the `qofe_report_present` gap above. |

**Summary:** 3 `pass`, 2 `partial`, 1 `gap-correct` — `score_qoe()` mechanical candidate was 4/6; corpus review of `ebitda_scenarios` lands 3 passing items, scored **3/5** against the precondition-adjusted denominator `candidate_total` **M=5** (Decision M2-C; 3/6 on the full structural N=6).
