# T11 decision log — ESC-M4-1 Decisions 2 and 4 (records only)

**Plan:** eval-consolidation-m4-onboarding-runbook v1.6 · **Date:** 2026-08-14 · **Tier:** architectural

## Ruling executed (charter Amendment A5 / ESC-M4-1 §5)

> **Decision 2 — (b) Eval-debt row** against Elder Care recording the lost `no_citation_source` provenance, `closes_when` the exclusion machinery is company-scoped.

> **Decision 4 — Registry row + eval-debt rows + operator disposition signoff.** Honest and matched to program precedent. The pilot's baseline (`baseline_7174e0399e29`) **stays** — it is not withdrawn or recomputed — but is **labeled** as computed over a gold set in which 12 of 48 scored intents cannot exceed ~1% recall@10, so no reader treats its mean recall as interpretable per-intent. No fresh Tier-3 spec escalation: the defect class is already described by `GAP-103` and `ESC-T12-1`.

## Chosen approach

1. Open Elder Care debt row `elder_care:global:no_citation_source_provenance_loss` with dual-clause `closes_when` (company-scoped exclusions + three annotation restorations) per ESC-M4-1 §6 items 2–3.
2. File Clearsulting F-03 as registry row `GAP-M4-1-clearsulting-bloated-filename-closure` with **per-intent** counts in the title (1273/1262/1085/1082 positives re-derived from `clearsulting.yaml`), plus 12 matching eval-debt rows and operator disposition signoff mirroring `signoffs/T12-bench-disposition.md`.
3. Label baseline on three named surfaces: registry row (tracked), T9 pilot addendum, runbook Step 6 caveat sentence — all cross-reference registry row and signoff.
4. Raise `open_debt_high_water_mark` to 14 before opens; patch `evidence_refs` after CLI (trust ref only from `open`).

## Alternatives rejected

- **Aggregate registry/debt row for all 12 intents:** Rejected — reproduces F-03's critique of the pilot signoff; packet Outputs (b)/(c) forbid it.
- **Re-bootstrap or withdraw `baseline_7174e0399e29` this round:** Rejected — ESC-M4-1 Decision 4 explicitly keeps the baseline standing with label only.
- **Set `source_id` on the new registry row:** Rejected — would require `source_manifest.yaml` edit outside Files to touch; used `source_id: null` (M4-native row pattern, cf. `M4-S3-item-36-clearsulting-pilot`).

## Assumptions made

- Twelve positive counts from `eval/retrieval/gold_labels/clearsulting.yaml` at HEAD match audit §7 F-03 (verified at execution: 1273, 1262×3, 1085, 1082×7; all `filename_closure`).
- `eval_debt open` CLI is the authoritative row opener; supplemental `evidence_refs` (ESC-M4-1, A5 charter, signoff, registry id) are ledger YAML edits in Files to touch because the CLI cannot accept custom refs.
- Commit `0a5b2f2` is cited in the A5/ESC-M4-1 narrative, not as a resolvable `evidence_refs` path (ratchet requires resolvable refs only).
- **`test_eval_debt.py` ratchet pin update:** Required for `pytest -q` green after HWM 14 (T9 precedent @ `d182f3c`); file was not in Files to touch — executed as coupled ledger hygiene; orchestrator may amend packet if strict confinement is required.

## Items deferred

- **Operator signature** on `signoffs/T11-clearsulting-bloated-gold-disposition.md` — ESC-M4-1 §6 item 4: orchestrator drafts, operator signs (placeholder in signoff).
- **Elder Care debt closure + annotation restoration + F-04** — T13 under A5 grant.
- **Hermetic test for per-intent registry title honesty** — deferred per §2.1 micro-pass (changelog).
