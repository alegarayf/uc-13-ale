# T9 — Clearsulting onboarding pilot walk

**Subtask:** T9 (M4 / S3 Company Onboarding Runbook — spec item 36)  
**Date:** 2026-08-14  
**Log tier:** architectural

## Chosen approach

- **Full eight-step walk** against `Clearsulting` in catalog `uc13_ale` per `eval/program/onboarding_runbook.md` after T10-bis landed (`231941b`).
- **Steps 3 and 5 on Databricks serverless** via workspace code sync + `jobs.submit` (operator script `.dev/t9_run_cluster.py`, not committed — outside Files to touch).
- **Gold committed** at `eval/retrieval/gold_labels/clearsulting.yaml` — 57 labels, 48 ready/partial, snapshot `uc13_ale:2417:2026-08-14`.
- **Six exemptions:** five `legal.*` → `corpus_absent` / `eliminates`; one `kpi.retrieve_bench_and_capacity` → `overlay_mismatch` with surface/coverage null, evidence from landed gold `notes` (two EBITDA PDF claims + registry `A-09`).
- **Step 6 skipped** per runbook §6; **Step 7** opened `clearsulting:global:promotion_inputs` eval-debt citing `OI-eval-harness-evaluate-promotion-clearsulting-gkf-spg`.
- **Harness baseline:** `baseline_7174e0399e29` (Databricks run `1086586115456516`).
- **Registry:** added closed row `M4-S3-item-36-clearsulting-pilot`; filed open runbook defect `RB-DEFECT-T9-3` for undocumented serverless execution path.

## Alternatives rejected

- **Patch bootstrap/harness code to fix serverless failures.** Rejected — T9 non-goals; recorded RB-DEFECT-T9-3 instead.
- **Fabricate gold for legal or overlay intents.** Rejected — kill criterion; exemptions + honest bootstrap_failed rows.
- **Invent promotion checklist scores.** Rejected — runbook §6 skip + eval-debt.
- **Exempt CQA bootstrap_failed intents without classification.** Rejected — left as honest gold; not Flag-6 ambiguous (generic zero-positive, not corpus-absent/overlay).

## Assumptions made

- T10-bis PDF adapter + skip-on-zero behavior is sufficient for Clearsulting KPI bootstrap without re-litigating RB-DEFECT-T9-2.
- `kpi.retrieve_bench_and_capacity` with `gold_status: ready` but `pdf_branch_unresolved` notes still warrants `overlay_mismatch` exemption (partial overlay gap legible in gold + exemption store).
- Workspace upload of local `eval/retrieval` tree reflects committed T10-bis code for cluster bootstrap.
- Harness `SystemExit(0)` causing job INTERNAL_ERROR is a platform wrapper issue, not harness failure — baseline id printed in logs.

## Items deferred

- **Runbook amendment for serverless cluster recipe (RB-DEFECT-T9-3).** Filed registry row; follow-on subtask owns runbook edit.
- **CQA zero-positive bootstrap_failed intents.** No exemption — may need corpus_thin classification in a future walk if harness metrics require it.
- **Adversarial: no hermetic test that committed Clearsulting gold matches live warehouse snapshot.** Walk evidence is Databricks run logs + committed YAML; CI uses Elder Care fixture only per §2.5 hermetic rule.
