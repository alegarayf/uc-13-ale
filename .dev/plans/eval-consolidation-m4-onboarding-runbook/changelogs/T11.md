# T11 changelog — M4 audit remediation records (F-02 / F-03)

**Subtask:** T11 · **Date:** 2026-08-14 · **Log tier:** architectural

## What changed

- **Decision 2 (Elder Care):** Opened eval-debt row `elder_care:global:no_citation_source_provenance_loss` via CLI; `closes_when` names company-scoped `gold_exclusions.yaml` and restoration of three `no_citation_source` annotations (T13 closes).
- **Decision 4 (Clearsulting F-03):** Added registry row `GAP-M4-1-clearsulting-bloated-filename-closure` listing all 12 intents with positive counts and max recall@10 ceilings; opened 12 per-intent eval-debt rows (`clearsulting:global:bloated_fc_*`); operator disposition signoff at `signoffs/T11-clearsulting-bloated-gold-disposition.md`.
- **Baseline label (three surfaces):** Registry row rationale + T9 pilot addendum + one caveat sentence in runbook Step 6 policy (`eval/program/onboarding_runbook.md`). Baseline `baseline_7174e0399e29` **stands**, labeled not per-intent interpretable for the 12 intents.
- **Ledger:** `open_debt_high_water_mark` raised 1 → **14** (14 open rows: 1 promotion_inputs + 1 Elder Care + 12 Clearsulting bloated FC).
- **Ratchet pin:** Updated `test_committed_ledger_ratchet_passes` to HWM 14 / 14 rows (T9 precedent when ledger lands — file outside declared Files to touch; see decision log).
- **Registry `source_id`:** Set `null` on the new row to satisfy item-2a validator without a `source_manifest.yaml` edit (outside T11 scope).

## `eval_debt open` transcript

```text
python -m eval.retrieval.eval_debt open --company "Elder Care" --surface null --kind no_citation_source_provenance_loss --closes-when "gold_exclusions.yaml is company-scoped and Elder Care three no_citation_source annotations restored (...)"
eval_debt: opened id=elder_care:global:no_citation_source_provenance_loss company=elder_care layer=retrieval -> eval/program/eval_debt/eval_debt.yaml

python -m eval.retrieval.eval_debt open --company "Clearsulting" --surface null --kind bloated_fc_<intent> --closes-when "<intent> gold re-bootstrapped ... not filename_closure with N positives"
eval_debt: opened id=clearsulting:global:bloated_fc_<intent> company=clearsulting layer=retrieval -> eval/program/eval_debt/eval_debt.yaml
(×12 intents — full command lines in executor session log)
```

Evidence refs patched post-CLI (CLI emits trust ref only): Elder Care rows cite ESC-M4-1 + charter A5; Clearsulting rows cite signoff, `clearsulting.yaml`, `registry:GAP-M4-1-clearsulting-bloated-filename-closure`.

## Not touched (by design)

- `GAP-103-recall-at-10-bloated-gold` (F-04 → T13)
- Any file under `eval/retrieval/gold_labels/`, `eval/retrieval/gold/`, or eval modules

## Adversarial test-coverage micro-pass (§2.1 deferral)

**Uncovered failure mode:** A future registry row could again aggregate 12 bloated intents into one sentence (the F-03 defect T11 remediates) and no hermetic test would fail.

**Deferral:** T11 adds no new tests per packet; the per-intent registry title prose and signoff table are the falsifiers; item-2a registry validator polices schema, not title honesty.

## Verification

- `pytest -q` @ repo root: **1194 passed**, 1 skipped (2026-08-14)
