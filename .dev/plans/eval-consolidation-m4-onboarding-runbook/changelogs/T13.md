# T13 changelog — Company-scoped gold exclusions and Elder Care restoration

**Subtask:** T13 · **Date:** 2026-08-14 · **Log tier:** architectural

## What changed

- **`gold_exclusions.yaml`:** Reshaped to `companies: {<slug>: {excluded: [...]}}`. Elder Care block restores five-intent launch population (profiler + four KPI `no_citation_source`); Clearsulting block is empty.
- **`bootstrap.py`:** `load_gold_exclusions(..., *, company_slug)` keyword-only; `GoldLabelBootstrap._gold_exclusions()` caches by folded company slug.
- **`elder_care.yaml`:** Restored three A5-granted rows (`kpi.retrieve_bill_rates_and_margins`, `kpi.retrieve_headcount_attrition`, `kpi.retrieve_pipeline_backlog`) to pre-`0a5b2f2` `aggregate_exclude` / `no_citation_source` shape.
- **`registry.yaml`:** `GAP-103-recall-at-10-bloated-gold` evidence line — population 5 again (F-04 closed by restored truth).
- **`eval_debt.yaml`:** Closed `elder_care:global:no_citation_source_provenance_loss` via `close_debt`; `open_debt_high_water_mark` remains **14** (monotonic ratchet per T11→T13).
- **Tests:** Per-company invariant, loader contract tests, restoration/metric-skip assertions, within-company contradiction falsifier.

## Constant movements (§5.4 item 13)

| Constant / test name | Old | New | Company | Reason |
|---|---|---|---|---|
| `EXPECTED_AGGREGATE_EXCLUDE_COUNT` (`test_gold_bootstrap.py`) | 2 | 5 | `elder_care` | Company-scoped loader now counts Elder Care's full exclusion population — contract restoration, not accommodation of 0a5b2f2 shrink |
| `LAUNCH_EXCLUDED_KPI` → `ELDER_CARE_LAUNCH_EXCLUDED_KPI` | 1 KPI intent (post-0a5b2f2) | 4 KPI intents | `elder_care` | Three `no_citation_source` KPI exclusions restored under A5; rename scopes the constant to Elder Care |
| `test_gold_exclusions_population_is_two_intents` → `test_elder_care_exclusion_population_is_five_intents` | asserts 2 | asserts 5 | `elder_care` | Committed artifact population is five after company-scoped reshape + restoration |

## Collateral test updates (outside declared Files to touch — T11 precedent)

- **`test_eval_debt.py::test_committed_ledger_ratchet_passes`:** open count **14 → 13** after T11 row closure; HWM stays 14; total rows stay 14.
- **`test_gold_kpi_pdf_branch.py`:** `GoldLabelBootstrap(..., company_name="Clearsulting")` — Clearsulting PDF citations must not run under Elder Care exclusions.

## Not touched (by design)

- `kpi_claim_intent_map.yaml`, `clearsulting.yaml`, `harness.py`, `trust_statement.py`

## Adversarial test-coverage micro-pass (§2.1)

**Uncovered failure mode:** Per-company invariant passes vacuously if claim-resolved set is derived globally instead of from each company's gold.

**Added falsifier:** `test_per_company_invariant_fails_on_within_company_contradiction` — synthetic exclusion of a claim-resolved Elder Care intent must fail the per-company disjointness check.

## Verification

- `pytest -q` @ repo root: **1201 passed**, 1 skipped (2026-08-14)
