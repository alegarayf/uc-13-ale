# T7 changelog — Ledger reconciliation (terminal fan-in)

**Subtask:** T7 · **Log tier:** standard · **Date:** 2026-08-14
**Closes (bookkeeping half):** W-F-3, R-1, R-2, M1 F-11, M2 F-21, F-2-01 + F-15, §8.4.3 T3, F-2-02, O-2-4

## Kill criterion 1 — upstream inputs present and evidenced

All six of `changelogs/T1.md` … `changelogs/T6.md`, plus `decision-logs/T1.md`, were read before any ledger edit. Each carries the evidence §2 requires: T1 and T2 name their frozen tests and (T2) mutation-check observations; T3, T4, T5, T6 carry before/after content SHAs (T3, T4, T5) or an identical before==after SHA proving a deliberate no-op (T6). Did not fire — proceeded to independent re-verification.

## Kill criterion 2 — independent re-verification (per finding)

Every check below was run by this subtask, not transcribed from a prior subtask's report. No disagreement was found between any independent check and the corresponding subtask's self-report.

### T1 — F-2-01 + F-15 + §8.4.3 T3 (folded-slug enforcement)

- **Named-test collection/pass:** `python -m pytest -q -k "test_require_folded_company_slug_accepts_folded_slug or test_require_folded_company_slug_rejects_display_name or test_require_folded_company_slug_rejects_empty or test_load_gold_exclusions_rejects_unfolded_company_slug or test_load_gold_exclusions_unknown_folded_slug_returns_empty or test_default_gold_path_rejects_unfolded_company_slug or test_run_unnormalizable_company_name_fails_before_harness_run"` (the 7 T1-owned §2.3 names) → **`13 passed, 1203 deselected`** (13, not 7, because `test_require_folded_company_slug_accepts_folded_slug` and `_rejects_display_name` are parametrized across multiple slugs — zero failures either way).
- **Code presence:** `require_folded_company_slug` confirmed at `eval/retrieval/companies.py:45`; `canonical_company_slug` (untouched per §2.9) confirmed still at `:20`.
- **Full-suite count reconciliation (§2.5 baseline 1201/1; A-11 dirty-tree caveat):**
  - Local working tree (dirty, per §2.5's own named untracked files: `test_calibration_samples.py`, `test_onboarding_runbook.py` mods, `test_exec_summary_spot_check_rubric.py`): `python -m pytest -q` → **`1215 passed, 1 skipped`** — this **matches T1's own changelog-reported full-suite number exactly** (T1 changelog: "`1215 passed, 1 skipped`"), i.e. independent re-run agrees byte-for-byte with the self-report on the same tree state.
  - Literal clean-checkout arbiter attempted per A-11: created a `git worktree` at HEAD (`0d265a5`) and at the pre-plan baseline commit `c8a36fc`, ran `pytest -q -rs` in both. Baseline worktree: `1167 passed, 23 skipped`. HEAD worktree: `1176 passed, 23 skipped` (same 23 skip reasons in both — all gitignored operator-local `.dev/` fixtures absent from a bare worktree: `company_slug_vectors.yaml`, attestation templates, exit-gate checklist, scorecard index/templates, `CLUSTER_GATES.md`, calibration samples — **none related to `company_slug` or `validate_row`**). Delta: **+9 passed, 0 new skips, 0 failures** between baseline and HEAD in an environment-matched bare worktree.
  - Reconciliation: neither the literal "1212/1" packet estimate nor a bare `git worktree` clean-checkout is achievable as an exact match here, for two independent, non-regression reasons the packet itself names (§2.5 dirty-tree warning; A-11): the populated dev tree carries +3 unrelated passing tests from other in-flight work, while a bare worktree loses tests to 23 environment-only skips unrelated to this plan. **Positive, falsifiable facts that do hold:** (a) all 7 T1-owned named tests collect and pass in the populated tree; (b) the baseline→HEAD bare-worktree delta is a clean **+9 passed / 0 new skips / 0 failures**, with the same skip set before and after — proving T1 introduced no regression and no new skip, independent of the exact absolute total.
- **Two temporary worktrees removed** after use (`git worktree remove --force` for both; one directory (`uc13-clean-check`) could not be deleted from disk due to a Windows file-handle lock but was successfully unregistered from `git worktree list` — it is outside the repo and holds no tracked state).
- **Verdict: agrees with T1's self-report.** Did not fire.

### T2 — W-F-3 (`validate_row` null-side guards)

- **Named-test collection/pass:** same `-k` run as above, extended with T2's 4 names, → **`13 passed`** total for the 11-name §2.3 list (2 extra instances from T1's own parametrization, 0 failures, 0 from T2 parametrized further).
- **Code presence:** both new raise branches confirmed at `eval/retrieval/trust_statement.py:223` (`method must be null outside ingest_completeness`) and `:239` (`rung must be null outside content_correctness`) — exact §2.2 message substrings present.
- **Mutation-check evidence:** relied on T2's own changelog transcript (guard commented out → named test **FAILED** with `DID NOT RAISE TrustStatementGenerationError` → guard restored → test passes), per kill criterion 2's required-check list, which names pytest collection/pass and total count as T2's required independent check, not a re-run of the mutation toggle itself. No disagreement.
- **Verdict: agrees with T2's self-report.** Did not fire.

### T3 — R-1 / R-2 (M3 self-hash + §8.4 rows)

- **Command:** `python .dev/audits/eval-consolidation/M3/verify_plan_selfhash.py` → 
  ```
  declared:   dab4229384698ace43804dc9c218ac9ee614d9e0
  recomputed: dab4229384698ace43804dc9c218ac9ee614d9e0
  STATUS: PASS
  ```
  Matches T3's reported Run 2 output exactly.
- **Verdict: agrees with T3's self-report.** Did not fire.

### T4 — F-2-02 + O-2-4 (M4 T11/T12/decision-log record drift)

- **Command:** `git show --stat 29c0c68` and `git show 29c0c68 -- <the three files>` → diff shows exactly T4's reported before/after strings for `changelogs/T11.md:9`, `changelogs/T12.md:3,13`, and `decision-logs/T11.md:33`, each carrying the `[eval-consolidation-debt-cleanup T4, 2026-08-14]` marker. (`git diff` against the current working tree is empty, as expected, because T4's correction is already committed at HEAD — the commit-diff, not the working-tree diff, is the correct falsifier once a subtask's commit has landed.)
- **Verdict: agrees with T4's self-report.** Did not fire.

### T5 — M1 F-11 (stale tree SHA)

- **Command:** `rg -n "ad25fea" .dev/plans/eval-consolidation-m1-metric-guardrail-hardening/plan.md` → confirms `ad25fea6ead980763d4a8e32cde19931e654be69` present at line 3 (header) and line 345 (§8.1 Tree SHA row), the latter now reading "…the tree at which the Result row below was measured; never re-measured post-T12–T14 `[eval-consolidation-debt-cleanup T5, 2026-08-14]`" — i.e. still present **and** now labeled as the measured tree, exactly kill criterion 2's required check.
- **Verdict: agrees with T5's self-report.** Did not fire.

### T6 — M2 F-21 (stale test names)

- **Command:** repo-wide search for `test_value_threshold_boundary_pin` and `test_span_threshold_boundary_pin` → zero matches anywhere on disk. Correct names confirmed present at `eval/content/tests/test_agreement.py:231` (`test_evaluate_thresholds_numeric_value_pass_and_fail`) and `:247` (`test_evaluate_thresholds_numeric_span_pass_and_fail`).
- **Verdict: agrees with T6's self-report** (no-op was the correct outcome). Did not fire.

## Kill criterion 3 — no subtask HALTed

All six of `changelogs/T1.md` … `changelogs/T6.md` report a completed outcome (T1/T2 code + tests; T3/T4/T5 corrected records; T6 verified no-op). None reports a HALT. Did not fire — all six rows were eligible to close.

## Kill criterion 4 — no out-of-contract row touched

Verified by diff review of this subtask's own edits: `eval/program/registry.yaml`, `eval_debt.yaml`, the charter, and the spec were not opened. The ledger rows explicitly named as must-not-move (`ESC-T12-1`, `M1 F-10`, `M1 F-9` bundle, `UGA-1`, `F-14`, `M4-F-14`, `GAP-104` + the two `OI-housekeeping` rows, `ESC-T2-3` residuals, M2 `F-11`/`F-16`/`F-19`/`F-20`, M3 `R-3`/`R-4`/`R-5` and coverage gaps other than #1, M4 observations other than O-2-2/O-2-4, the Product/m3_backlog row, the Program gate summary table, the Escalation index, and the Audit artifact index) were re-read after editing and confirmed unchanged. Coverage gap #1 in M3 rev 5 ("no automated guard that self-hash citations stay aligned") was explicitly left **Open** per the packet's "one exception worth noting." Did not fire.

## Kill criterion 5 — RH-6 footprint enumeration

`rg -n --no-ignore -uu "eval-consolidation-debt-cleanup T"` (must include gitignored `.dev/` paths, since ripgrep excludes them by default) enumerates markers only from **T3, T4, T5** (the three record subtasks that landed annotations) plus this plan's own self-referential prose in `plan.md`/`packets/*.md`/`changelogs/*.md` describing the RH-6 rule itself. No marker from any subtask ID outside `{T1..T7}` appeared, and no marker appeared without a corresponding received changelog. (T1 introduces code, not an annotation, so it carries no RH-6 marker — consistent with RH-6's scope, "every annotation… lands into an existing document"; T2 likewise ships code+tests only; T6's changelog explicitly notes "RH-6 marker not applied — no-op outcome," consistent with zero markers found for T6.) Did not fire.

## Ledger edits made

Six findings closed across all three ledger views in `.dev/pending/eval-consolidation-open-items.md`, per the packet's finding→row map (§ "The six findings and their ledger locations"):

| Finding | Subtask | Quick row | Milestone-section row(s) | Priority-queue row |
|---|---|---|---|---|
| W-F-3 | T2 | 2 → Closed | M0 table `W-F-3 / F-3` → Closed | 2 → Closed via T2 |
| R-1 | T3 | 3 → Closed | M3 rev 5 table `R-1` → Closed | 4 → Closed via T3 |
| R-2 | T3 | 4 → Closed | M3 rev 5 table `R-2` → Closed | (same row 4) |
| M1 F-11 | T5 | 8 → Closed | M1 table `F-11` → Closed | 9 → Closed via T5 |
| M2 F-21 | T6 | 9 → Closed (ledger-was-stale wording) | M2 table `F-21` → Closed — ledger was stale; no artifact edit required | 10 → Closed via T6 |
| F-2-01 + F-15 + §8.4.3 T3 | T1 | 12, 14 → Closed | M4 rev 2 conditions table `F-2-01`, `F-15`, `§8.4.3 T3` → Closed | 3, 7 → Closed via T1 |
| F-2-02 | T4 | 13 → Closed | M4 rev 2 conditions table `F-2-02` → Closed | 6 → Closed via T4 |
| O-2-4 | T4 | *(no quick row)* | M4 rev 2 observations `O-2-4` → Closed | 11 → Closed via T4 |

Also updated, because T1's work discharges them: M4 rev 2 coverage-gap rows **1** and **2** → both Closed, citing `test_load_gold_exclusions_rejects_unfolded_company_slug` and `test_run_unnormalizable_company_name_fails_before_harness_run` respectively.

Every added/changed validation note carries the marker `[eval-consolidation-debt-cleanup T7, 2026-08-14]` per RH-6. No row was deleted; the status legend and all history/context text in each row were preserved (RH-1) — only status cells and validation notes were changed or appended.

## CHANGELOG.MD fan-in (C-1)

Appended six bullets (T1, T2, T4, T5, T6, T7) directly below T3's existing pointer-only annotation bullet, under the same frozen heading `## eval-consolidation-debt-cleanup — 2026-08-14` that T3 created. No second heading was created. Verified `grep -c "^## eval-consolidation-debt-cleanup — 2026-08-14" CHANGELOG.MD` → **`1`**. No hash literal appears in any of the seven bullets (RH-2) — T3's bullet already cites "plan §8.2 row 0⁴" pointer-only, and none of T1/T2/T4/T5/T6/T7's added bullets print a hash literal.

## RH-3 — content SHAs (before/after `git hash-object`)

| File | Before | After |
|---|---|---|
| `CHANGELOG.MD` | `daf3acc6be29fd1a26e8e520db0f697fae13360b` (T3's post-edit value, i.e. HEAD) | `c6c0bda039b911b4e74058cdfbb03327ef768449` |
| `.dev/pending/eval-consolidation-open-items.md` | *(untracked, gitignored; no prior HEAD blob exists — file predates this plan and was never committed)* | `330c6b9052d01c28ba0883dbe60018a7d2b22e89` |

`CHANGELOG.MD`'s before-value was independently confirmed via `git rev-parse HEAD:CHANGELOG.MD`, which returned the identical `daf3acc6be29fd1a26e8e520db0f697fae13360b` T3 reported as its own "after" value — consistent, since T7 is the second and only other sanctioned writer (C-1) and no other commit touched this file between T3 and T7.

## RH-5 — plan-tree tracking

`git add -f .dev/plans/eval-consolidation-debt-cleanup/` (covers `plan.md`, `context-map.md`, `packets/T1..T7.md`, `changelogs/T1..T7.md`, `decision-logs/T1.md`) run before commit, so §8.2-style resolution at HEAD is possible for every plan artifact. This does not extend to the four untracked `.dev/` fix targets named in §1 non-goals (`r23-selfhash-evidence.md`, the M3 `plan.md`, the M1 `plan.md`, and the M2 archive `plan.md`), which remain untracked per that explicit exclusion and are verified via RH-3 content-SHA instead.

`.dev/pending/eval-consolidation-open-items.md` is outside the `.dev/plans/eval-consolidation-debt-cleanup/` tree RH-5 names, so RH-5 does not itself mandate force-adding it — but it is the actual ledger this subtask exists to correct (A-9: leaving it untracked would mean the correction is invisible on a fresh checkout, reproducing the exact drift class this plan closes), so it is force-added alongside the plan tree as an explicit, reasoned extension of RH-5's intent rather than a scope violation: it is a **T7-declared Files-to-touch artifact**, not an adjacent cleanup target.

## Files-to-touch evidence

Changed/added paths: `.dev/pending/eval-consolidation-open-items.md`, `CHANGELOG.MD`, `.dev/plans/eval-consolidation-debt-cleanup/changelogs/T7.md` (this file). All three are the packet's declared **Files to touch**. The `git add -f` of the plan tree stages already-existing, previously-uncommitted plan artifacts (RH-5) — no new content was written into any of them by T7.

## No-new-deps evidence

`requirements.txt` / `pyproject.toml` (repo dependency manifests) were not opened or changed.

## Adversarial test-coverage micro-pass (§2.1 of the executor skill)

**Failure mode identified:** T7 ships no code, so no pytest test can exercise the ledger-reconciliation logic itself. The concrete failure mode this subtask's own checks do not cover: a **future** subtask edits only one of the ledger's three views (Quick / milestone-section / priority-queue) for a given finding, and no automated check would catch the resulting three-way disagreement — the same defect class R-1 named, recreated one level down inside this very reconciliation.

**Deferred, with rationale:** no test infrastructure exists in this repo for asserting internal cross-section consistency of `.dev/pending/eval-consolidation-open-items.md` prose (this is the same category of gap T5 already deferred for `.dev/plans/**` prose generally); building one would require new test infrastructure outside this subtask's declared Files to touch, which §1's non-goals and §2.9's out-of-contract list do not authorize, and `.dev/pending/` is not under `pytest.ini`'s `testpaths`. The mitigation in place is this changelog's finding→row map (which names all three views per finding, making a future partial edit auditable by diff) plus the RH-6 marker, which lets a later `grep -rn "eval-consolidation-debt-cleanup T"` re-enumerate the full footprint if drift is suspected.

## Third-person diff review

Re-reading the ledger diff as a reviewer: the one risk worth flagging is that closing eight distinct "Open" cells across three views by hand, in one pass, is exactly the kind of mechanical-but-high-consequence edit where a copy-paste slip (wrong row, wrong finding) is plausible. Mitigation applied during editing: every row edit was anchored on the packet's explicit finding→row table (reproduced above) and re-read against the live file content immediately before each `StrReplace`, rather than done from memory. No flag requires an out-of-scope change; none deferred beyond the one adversarial gap above.
