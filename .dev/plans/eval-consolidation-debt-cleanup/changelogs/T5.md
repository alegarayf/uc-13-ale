# T5 changelog — M1 plan §8 stale tree-SHA record correction

**Subtask:** T5 · **Log tier:** standard · **Closes:** M1 F-11 · **Marker:** `[eval-consolidation-debt-cleanup T5, 2026-08-14]`

## Kill criterion 1 — git facts re-verified at execution start

All three facts re-verified and matched the packet's planning-time values exactly:

- `git merge-base --is-ancestor bf853e389c3f8c1c040f49120d7d3165c173be3a 5b447058d41f6092b0569199470039a6f3b62121` → exit `0`.
- `git merge-base --is-ancestor 5b447058d41f6092b0569199470039a6f3b62121 HEAD` → exit `0`.
- `git log --oneline --all --grep="T13"` → no M1 T13 commit; only M4's `5bbd3a1 T13: company-scope gold exclusions and restore Elder Care annotations` and the unrelated `0d760b9 T13: Update tests for v1.1.0...`.

Kill criterion 1 did not fire. Proceeded to edit.

## Content SHA (RH-3 falsifier)

File: `.dev/plans/eval-consolidation-m1-metric-guardrail-hardening/plan.md` (untracked/gitignored `.dev/`, so `git diff` is not available — content SHA is the only falsifier).

- Before: `c7d81fb339b587677f55facb9cea042bde30ac32`
- After: `bba45832d4ad4b0e2a610ac763397e475dee70f6`

(Different — a real edit landed, not a no-op.)

## Edited lines — before / after

### Header, line 3

**Before:**

> **Milestone:** M1 — Metric & Guardrail Hardening · **Spec slice:** S1 (spec §6 S1) · **Plan version:** 2.1 (2026-08-11) · **Status:** Complete + §7.2 amendments pending — audit rev 1 verdict **fail** (F-1/F-2 blocking); §8 below is the v2.0 snapshot at tree SHA `ad25fea6ead980763d4a8e32cde19931e654be69` and will be re-issued after T12–T14 land and re-audit rev 2 re-grades

**After:**

> **Milestone:** M1 — Metric & Guardrail Hardening · **Spec slice:** S1 (spec §6 S1) · **Plan version:** 2.1 (2026-08-11) · **Status:** Complete + §7.2 amendments pending — audit rev 1 verdict **fail** (F-1/F-2 blocking); §8 below is the v2.0 snapshot at tree SHA `ad25fea6ead980763d4a8e32cde19931e654be69` ~~and will be re-issued after T12–T14 land and re-audit rev 2 re-grades~~ **[eval-consolidation-debt-cleanup T5, 2026-08-14]:** T12 landed at `bf853e3`, T14 landed at `5b447058`, and re-audit rev 2 graded pass-with-conditions — but M1's T13 produced no tracked commit (its diff was entirely gitignored `.dev/` plan prose), so no single post-T12–T14 SHA exists to re-anchor a fresh handoff to. §8 below was never re-issued or re-measured at the post-T14 tree; it remains the v2.0 snapshot measured at `ad25fea`. F-11 is closed as record hygiene, not by re-measurement (an explicit non-goal of the closing subtask).

### §8.1 Tree SHA row, line 345

**Before:**

> `| Tree SHA | `ad25fea6ead980763d4a8e32cde19931e654be69` (T11 commit) |`

**After:**

> `| Tree SHA | `ad25fea6ead980763d4a8e32cde19931e654be69` (T11 commit) — **the tree at which the Result row below was measured; never re-measured post-T12–T14 [eval-consolidation-debt-cleanup T5, 2026-08-14]** |`

### §8.1 Result row (line 349) and Execution span row (line 350)

Re-verified for consistency, per Files to touch. Both already correctly cite `ad25fea` as the measured tree (`983 passed / 6 skipped / 0 failed` and `T2–T11 commits 65189be…ad25fea`) — no edit required; leaving them untouched is itself the correct outcome, since editing them would risk drifting the measured-tree claim away from `ad25fea` (forbidden by kill criterion 2).

## Why annotate rather than swap

The `983 passed / 6 skipped` result and the `T2–T11 … ad25fea` execution span were measured at tree `ad25fea`, not at `5b447058` or any other post-T14 commit. Swapping the Tree SHA row to a later commit while leaving the Result row unchanged would assert that a verification ran at a tree where it never did — a fabricated §8.1 snapshot, and a worse defect than the stale-citation finding (F-11) this subtask closes. The correction therefore keeps `ad25fea` as the anchor for both the Tree SHA and Result rows, and instead resolves the header's dangling "will be re-issued" promise by stating what actually happened: T12 and T14 landed, rev 2 re-audit graded, and no re-measurement occurred because M1's T13 left no tracked commit to re-anchor a fresh v2.1 snapshot to (a structural oddity worth recording, not resolving — the annotation approach makes resolving it unnecessary). If a genuine v2.1 §8 (clean-checkout re-measurement, new tree SHA, new §8.2 chain) is wanted later, that is an explicitly separate, larger subtask — not this one.

## Kill-criterion evidence

1. **Satisfied — did not fire.** Re-verification commands above matched planning-time facts exactly.
2. **Satisfied — did not fire.** §8.1's Tree SHA row still reads `ad25fea6ead980763d4a8e32cde19931e654be69`, and the Result row (`983 passed / 6 skipped / 0 failed`) was left unchanged and still sits under that same Tree SHA row — no result is asserted against any other SHA.
3. **Satisfied — did not fire.** No `pytest` command was executed against `5b447058` or any other tree as part of this subtask; the residual (§8 never re-measured post-T14) is reported in the header annotation, per the §1 non-goal.
4. **Satisfied.** Literal `ad25fea6ead980763d4a8e32cde19931e654be69` is present in both line 3 (header) and the §8.1 Tree SHA row after edit.
5. **Satisfied — did not fire.** §8.2, §8.3, and §8.4 were not read for editing and are not part of the diff; only header line 3 and §8.1's Tree SHA row were changed.

## Scope note

Files to touch also named §8.1's Result (`:349`) and Execution span (`:350`) rows as rows "that must stay consistent" with the Tree SHA row — this was a verification obligation, not necessarily an edit obligation. Both were confirmed already consistent (they cite `ad25fea` results/commits) and were left untouched.

## Adversarial test-coverage micro-pass (§2.1 of the executor skill)

This subtask is record-only (§2.7 RH-1–RH-6); it introduces no code path, so no test can exercise it via `pytest`. **Failure mode not covered:** a future edit to this same file could silently re-introduce a Tree-SHA/Result mismatch (kill criterion 2's failure mode) without any automated check catching it, since `.dev/plans/**` prose is not covered by any test. **Deferred, with rationale:** no test infra exists in this repo for asserting internal consistency of `.dev/` plan prose (RH-3's content-SHA falsifier is the closest analog, and it is already applied above); adding one would require new test infrastructure outside this subtask's Files to touch, which the packet's non-goals and §2.9 out-of-contract list do not authorize. The mitigation in place is the RH-6 marker plus this changelog's before/after quotes, which make any future silent regression auditable via `grep -rn "eval-consolidation-debt-cleanup T"`.

## Files touched (Files-to-touch evidence)

- `.dev/plans/eval-consolidation-m1-metric-guardrail-hardening/plan.md` (header `:3`, §8.1 Tree SHA row `:345`) — subset of declared Files to touch.
- `.dev/plans/eval-consolidation-debt-cleanup/changelogs/T5.md` (this file, newly created) — declared Files to touch.

No other file was read for editing purposes or modified.

## No-new-deps evidence

No dependency manifest in this repo (`requirements.txt`, `pyproject.toml`, or equivalent) was touched. This subtask made no code change of any kind.
