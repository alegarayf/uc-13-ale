## eval-consolidation-debt-cleanup — 2026-08-14

- T2 (`validate_row` §16 null-side hardening): Added two raise branches in `validate_row` — reject non-null `method` outside `ingest_completeness` and non-null `rung` outside `content_correctness` when attestation is `attested` or `partial`; added four frozen hermetic tests in `test_trust_statement.py`. Closes W-F-3 by mirroring the existing `content_surface` layer-appropriateness pattern already enforced in `__post_init__`.

### Mutation checks (KC2)

- **Method guard:** Temporarily commented out `if row.layer != "ingest_completeness" and row.method is not None` → `test_validate_row_rejects_method_outside_ingest_completeness` **FAILED** (`Failed: DID NOT RAISE TrustStatementGenerationError`); guard restored → test passes.
- **Rung guard:** Temporarily commented out `elif row.layer != "content_correctness" and row.rung is not None` inside the `attested`/`partial` block → `test_validate_row_rejects_rung_outside_content_correctness_on_attested` **FAILED** (`Failed: DID NOT RAISE TrustStatementGenerationError`); guard restored → test passes.

### Adversarial coverage deferral

- **Deferred:** Off-layer `rung` with `attestation="partial"` (same `attested`/`partial` branch as the attested falsifier; no separate test added).

### Test gate

- `python -m pytest -q` → **1205 passed, 1 skipped** (dirty tree; +4 vs 1201 baseline from this subtask).
