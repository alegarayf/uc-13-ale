# T3 — Execute merge-rank enhancement runs (4 companies)

**Plan:** eval-signal-foldback-m1-retrieval-loop · **Subtask:** T3 · **Date:** 2026-08-20

## Chosen approach

1. **Live T2 preflight first** — `python -m eval.retrieval.scripts.preflight_epoch_pins` against `uc13_ale`; all four companies passed (`baseline_valid` implicit via `harness_status=complete`, `gold_snapshot_match=True`, `registry_hash_match=True`).
2. **Per-company gate-eligible intents** — computed locally via `gate_eligible_intent_ids(load_gold_labels(default_gold_path(slug)), company_name=..., catalog=uc13_ale)`; passed as `--affected-intents` CSV to T1's frozen `harness-run` CLI.
3. **Enhancement submit** — one serverless job per company:
   - `--run-type enhancement`
   - `--ablation-config '{"arm": "merge_rank_off"}'`
   - `--baseline-ref-run-id` = D4 sibling baseline per company
   - Delta backend hardcoded (T1 driver); success via `HARNESS_RUN_ID=` log grep.
4. **`validate-baseline`** — attempted locally first (orchestrator); **blocked by no local `pyspark`** for `--store-backend delta`. Re-ran on Databricks serverless with a thin driver calling `harness_cli validate-baseline`; all four companies printed `baseline_ref validation passed` (semantic exit 0).
5. **Kill criterion (c)** — queried `uc13_ale.ops.retrieval_harness_deltas`; non-zero `delta` rows exist on every company → **no plan-wide HALT**.

## Run outcomes

| Company | Baseline ref | Enhancement run_id | Intents | Run gate_pass | validate-baseline (semantic) | Skipped |
|---------|--------------|-------------------|---------|---------------|------------------------------|---------|
| Elder Care | `baseline_2fa3a9056bd0` | `enhancement_61c90e6068bb` | 52 | `false` | 0 (remote) | — |
| Clearsulting | `baseline_488f70f13570` | `enhancement_11db0ef8a7ea` | 37 | `false` | 0 (remote) | — |
| GKF | `baseline_7510d1d14449` | `enhancement_540d38fe78a9` | 22 | `false` | 0 (remote) | — |
| SPG | `baseline_3992534e412f` | `enhancement_4e61af4d4c54` | 38 | `false` | 0 (remote) | — |

**Preflight stdout (2026-08-20 live):**

```
company=Elder Care baseline_run_id=baseline_2fa3a9056bd0 harness_status=complete gold_snapshot_match=True registry_hash_match=True
company=Clearsulting baseline_run_id=baseline_488f70f13570 harness_status=complete gold_snapshot_match=True registry_hash_match=True
company=GKF baseline_run_id=baseline_7510d1d14449 harness_status=complete gold_snapshot_match=True registry_hash_match=True
company=SPG baseline_run_id=baseline_3992534e412f harness_status=complete gold_snapshot_match=True registry_hash_match=True
```

## Delta summary (kill criterion c)

`kill_criterion_c_all_zero`: **false** — measurable dispatch deltas on all four companies.

| Company | Delta rows | Non-zero delta rows | Intents with any non-zero delta |
|---------|------------|---------------------|----------------------------------|
| Elder Care | 107 | 20 | 15 |
| Clearsulting | 74 | 20 | 12 |
| GKF | 44 | 9 | 7 |
| SPG | 76 | 11 | 7 |

**Per-intent highlights (positive gate_pass on at least one metric):**

- Elder Care: `fta.ebitda.q4_addback_schedule` (mrr +0.5), `fta.opex.q1_financial_statements` (precision_at_10 +0.125)
- Clearsulting: `bma.retrieve_model_changes_and_dependencies` (mrr +0.05)
- GKF: `bma.retrieve_model_changes_and_dependencies` (mrr +0.667)
- SPG: `bma.retrieve_model_changes_and_dependencies` (mrr +0.167), `bma.retrieve_revenue_visibility` (mrr +0.667)

Run-level `gate_pass=false` on all companies is expected for a degrading ablation arm; per-intent mixed signal matches plan risk note (§4).

## Report JSON paths (workspace — T4 to commit locally)

Written on Databricks workspace repo root during harness runs (not committed in T3):

- `eval/retrieval/reports/enhancement_61c90e6068bb.json`
- `eval/retrieval/reports/enhancement_11db0ef8a7ea.json`
- `eval/retrieval/reports/enhancement_540d38fe78a9.json`
- `eval/retrieval/reports/enhancement_4e61af4d4c54.json`

Ops rows also in `uc13_ale.ops.retrieval_harness_runs`, `_results`, `_deltas`.

## Raw log captures

Under `.dev/plans/eval-signal-foldback-m1-retrieval-loop/logs/` (gitignored runtime artifacts):

- `T3_preflight_stdout.txt`
- `T3_orchestrator_live.log`, `T3_orchestrator_results.json`
- `T3_harness_run_{Elder_Care,Clearsulting,GKF,SPG}.log`
- `T3_validate_*_remote.log`, `T3_validate_remote_summary.json`
- `T3_delta_summary.json`

## Alternatives rejected

- **Fresh baseline for any preflight-failing company:** Not triggered (all passed); would violate plan non-goal.
- **Local-only `validate-baseline`:** Rejected at execution — `AGENTS.md` / repo convention: no local `pyspark`; delta store requires Spark. Remote serverless validate is the operational equivalent.
- **`--run-type ablation` without affected intents:** Rejected — plan resolves Flag 2 by submitting as `enhancement` with gate-eligible `--affected-intents` and merge_rank arm JSON.

## Assumptions made

- T1 `harness-run` CLI surface matches plan §2 verbatim (verified before submit).
- T2 preflight static pins agree with T3 live `validate_baseline_ref` when gold/registry unchanged between runs (verified — no T2/T3 discrepancy).
- `merge_rank_off` arm is exercised end-to-end via lazy `dispatch_retrieval` import (first live exercise on D4 epoch; non-zero deltas confirm arm is not a no-op).

## Items deferred

- **Commit enhancement report JSONs:** T4 scope per plan non-goal for T3.
- **Orchestrator local validate exit codes:** Recorded as 1 (pyspark missing); decision log uses remote semantic exit 0. Follow-on: optional T1/T3 helper for serverless `validate-baseline` (out of T3 files-to-touch).
- **Adversarial test coverage:** Runtime-only subtask — no new tests per packet §2 Tests contract.
